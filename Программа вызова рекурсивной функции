# Данная программа получает на вход команды создания пространств и наполнения их переменными 
# и выводит результат запросов, какому пространству принадлежит конкретная переменная. 
# Программе на вход подаются следующие запросы:

# 1.create – создать новое пространство (множество) с именем внутри пространства
# 2.add – добавить в пространство переменную
# 3. get – получить имя пространства, из которого будет взята переменная при запросе из пространства, 
# или None, если такого пространства не существует 

# ФОРМАТ ВХОДЯЩИХ ДАННЫХ:
# В первой строке дано число n (1 ≤ n ≤ 100) – число запросов. В каждой из следующих n строк дано по одному запросу. 
# Запросы выполняются в порядке, в котором они даны во входных данных. Имена пространства имен и имена переменных 
# представляют из себя строки длины не более 10, состоящие из строчных латинских букв. 

#ФОРМАТ ВЫХОДНЫХ ДАННЫХ: 
#Для каждого запроса get в отдельной строке выводится его результат.
